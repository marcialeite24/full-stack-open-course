# Course Info (Extended)

An extended version of the course info app from Part 1, now capable of rendering multiple courses from an array using `.map()`.

The focus here was on rendering collections — using `.map()` to turn data into components, understanding why React requires a unique `key` prop on each list item, and computing totals with `.reduce()`.

## Stack

- React + Vite
